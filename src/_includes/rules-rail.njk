<!-- THE RULES — subreddit-style rail. Rendered + enforced by /assets/js/rules.js.
     Rules config lives in rules.js so Barrie can edit copy without touching markup. -->
<aside class="rules-rail" id="rules-rail" aria-label="The Rules">
    <button class="rules-rail__chip mono" aria-expanded="false" aria-controls="rules-rail-body">📌 THE RULES</button>
    <div class="rules-rail__body" id="rules-rail-body">
        <div class="eyebrow rules-rail__header">THE RULES</div>
        <ol class="rules-rail__list">
            <!-- populated by rules.js -->
        </ol>
        <p class="rules-rail__status mono" aria-hidden="true">🔒 lurking…</p>
    </div>
</aside>
<div class="automod-toast" id="automod-toast" role="status" aria-live="polite" hidden></div>
