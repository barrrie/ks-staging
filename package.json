{
  "name": "kind-stranger-website",
  "version": "2.0.0",
  "private": true,
  "description": "Kind Stranger — community marketing agency",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
